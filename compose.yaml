services:
    discord-music:
        build:
            context: .
        env_file:
            - .env
        volumes:
            - /home/bycrookie/private/temp/cache:/data
        platform: "linux/amd64"
#        platform: "linux/arm64"